<%= render "shared/navbar"  %>
<p style="color: green"><%= notice %></p>
<div class="categories">
<h2 class="categories_title">Categories:</h2>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <p id="newcategory_button">
      <%= link_to "Show this category", category, class: "btn-form" %>
    </p>
  <% end %>
</div>

<%= link_to "New category", new_category_path, class: "btn-category" %>


